---
title: "Documentation for `get_bw_score` Function"
author: "Md Aminul Islam Prodhan"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Introduction

The `get_bw_score` function is designed to normalized the the body-weight (BW) of each subject (animal) using Z-scores 
method with comparisons made against the respective control group for each study.

### Z-Score Calculation

The Z-score normalization is performed as shown in Equation 1:

\[
Z_{s,i} = \frac{x_{s,i} - \mu_{s,c}}{\sigma_{s,c}}
\]

**Where:**
- \(x_{s,i}\) is the observed endpoint value for individual \(i\) in study \(s\),
- \(\mu_{s,c}\) is the mean value of the observed endpoint for the control group \(c\) in study \(s\),
- \(\sigma_{s,c}\) is the standard deviation of the observed endpoint for the control group \(c\) in study \(s\),
- \(s\) is the study identifier,
- \(i\) refers to an individual animal in the study,
- \(c\) refers to the control-treated group of animals within the study.

## Parameters

| **Parameter**               | **Type**       | **Description**                                                                                             | **Default** |
|------------------------------|----------------|-------------------------------------------------------------------------------------------------------------|-------------|
| `studyid`                   | Mandatory, character | The study ID for which the BW score is to be calculated. If `NULL`, all studies in the database are analyzed. | `NULL`      |
| `path_db`                   | Mandatory, character | The path to the database file containing study data.                                                        | —           |
| `fake_study`                | Optional, Boolean   | Indicates whether the study was generated by the `SENDsanitizer` package.                                   | `FALSE`     |
| `use_xpt_file`              | Mandatory, Boolean   | If `TRUE`, the function processes study data using `.xpt` files.                                            | `FALSE`     |
| `master_compiledata`        | Optional, character | The path to an additional database or compiled data file for analysis. If `NULL`, only the primary database is used. | `NULL` |
| `return_individual_scores`  | Optional, Boolean   | If `TRUE`, the function returns individual scores for each record in the study.                             | `FALSE`     |
| `return_zscore_by_USUBJID`  | Optional, Boolean   | If `TRUE`, the function returns z-scores by `USUBJID` (unique subject identifiers).                         | `FALSE`     |

## Returns

A `data.frame` containing the calculated BW scores. The structure of the output depends on the provided parameters:
- If `return_individual_scores = TRUE`: Returns individual scores for each record.
- If `return_zscore_by_USUBJID = TRUE`: Returns z-scores by `USUBJID`.
- Otherwise, a summarized BW score for the specified `studyid`.


## Example Usage

```r
# Example 1: Basic usage
get_bw_score(studyid = '1234123', path_db = 'path/to/database.db')

# Example 2: Include individual scores
get_bw_score(studyid = '1234123', path_db = 'path/to/database.db', return_individual_scores = TRUE)

# Example 3: Include z-scores by USUBJID
get_bw_score(studyid = '1234123', path_db = 'path/to/database.db', return_zscore_by_USUBJID = TRUE)
```

# Implementation Details

The function filters rows where the `prob` column exceeds the `binding_threshold` and computes the score using vectorized operations. This ensures efficiency and scalability for larger datasets.

# Additional Notes

- The function assumes the input data frame is correctly formatted. Pre-processing steps (e.g., missing value handling) should be performed beforehand.
- Users can customize the `binding_threshold` to match the specificity requirements of their analysis.
